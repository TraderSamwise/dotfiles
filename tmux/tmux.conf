# Remap prefix from Ctrl+B to Ctrl+A (easier to reach)
unbind C-b
set -g prefix C-a
bind C-a send-prefix

# Mouse support (click to switch panes, scroll, resize)
set -g mouse on

# Ctrl+A then R to force full redraw
bind R refresh-client \; display "Refreshed"
